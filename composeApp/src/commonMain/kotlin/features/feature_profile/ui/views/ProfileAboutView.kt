package features.feature_profile.ui.views

import androidx.compose.material.Text
import androidx.compose.runtime.Composable
import theme.Theme

@Composable
fun ProfileAboutView() {
    Text(text = "Hello, About", color = Theme.colors.primaryText)
}
