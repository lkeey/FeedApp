package features.feature_profile.ui.views

import androidx.compose.material.Text
import androidx.compose.runtime.Composable
import theme.Theme

@Composable
fun ProfileVideosView() {
    Text(text = "Hello, Videos", color = Theme.colors.primaryText)
}
